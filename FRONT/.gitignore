# Dependencias y build
node_modules
.next
out
dist
*.tsbuildinfo

# Datos de usuario / bóveda (API Next crea vault_data junto al repo o en FRONT)
vault_data/
vault_data

# Claves y contenedores (por si se copian aquí por error)
*.pem
*.key
*.vault

# Secretos (URLs de API, claves, etc.)
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local
